rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /news/{document=**} {
      allow read: true;  // Allow public reading of news stories
      allow write: if request.auth != null;  // Only authenticated users can write
    }
  }
} 